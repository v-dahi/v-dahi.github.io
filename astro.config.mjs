// DEPLOY COMMANDS:
// git add . && git commit -m "update" && git push origin main
// npm run deploy

import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://v-dahi.github.io',
});